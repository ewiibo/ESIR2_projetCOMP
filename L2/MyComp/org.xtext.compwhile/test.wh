function f1: 
read X,Y
%
  nop;
  T := Z;
  A := (cons (cons (tl nil) (hd Y)) nil);
B,C := (hd Y), (cons nil nil);
for A do
	C:=nil
od

%
write Y

function main:
read X % 
A:= nil;
B:= (cons A (hd nil));
if B then 
A:=B else B:=nil fi;
B:=(cons nil (cons nil (cons nil nil)));
while B
do
B:= (tl B);
A:=B
od ;  A := (cons nil (cons (tl nil) (hd Y)) );
B,C := (hd Y), (cons nil nil);
for A do
	C:=nil
od;
if B then 
A:=B fi
%
write X

