function 
f1
 :
  read
   Var1,			 Var2  
%

foreach V,
		V2
		 in
		  nil
		    do
		    		
	nop
od;
foreach V in nil do	foreach V in nil do foreach V, N, K in nil do nop od; foreach B, B, C in ( list n lkj mm ) =? nil do 
			foreach C, J in ( cons ( hd ll ) mlk LL! ) =? (cons ( list aa) =? kj) do
				nop od; foreach A,B,C,D in ( list ( cons lk LL ) jkjk ( hd ljj ) ( tl hjkj ) ) do 
				foreach K,K,K,K in nil do
					nop
		 			od
			od
					od
							od	
od %
write 	
				Var1
