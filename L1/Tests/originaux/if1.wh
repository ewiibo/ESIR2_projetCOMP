function
f1
:
read 
Var1
,

 Var2  
%
if (a (
		
			cons b hkj 
			
			lkl nil
	 nil ( cons nil )
) AA

						
						
						 ( hd lll )					 ( list ( list a ) ) ) =? kk  then  nop
fi;


if (list a a AA! BB?
	
	
	( 		 tl
		
		hhj
	) ( cons lklk )
) =? 
	

( cons ( cons (
	
	
	
	
	hd a
) ) jkl (tl kj)  )  then
	if nil  then
		A , B := ( cons ( list a									 =? b ) )
											else
		if ( list nil )	
		
 then
			nop 
		else 
			AA :=
									 nil  fi; if A then
			nop	fi
fi
else  NN := ( list ( cons MMM ) nil nil  mlk )   
fi


%
write Var1